# 问题清单与复现

## Open findings

| ID | Severity | 状态 | 类型 | 证据 |
|---|---:|---|---|---|
| `GBV6-07-001` | P1 | OPEN | LOADCHUNK 数据完整性 | `.codex/gemini-bloom-audit/v6/evidence/stage07/findings.md` |
| `GBV6-07-002` | P1 | OPEN | LOADCHUNK 持久化/fullsync 数据完整性 | `.codex/gemini-bloom-audit/v6/evidence/stage07/findings.md`, `.codex/gemini-bloom-audit/v6/evidence/stage09/replica/loading_partial_fullsync.log` |
| `GBV6-03-001` | P2 | OPEN | RDB/wire 资源边界 | `.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md` |
| `GBV6-03-002` | P2 | OPEN | RDB/wire expansion 边界 | `.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md` |
| `GBV6-05-001` | P2 | OPEN | Linux/GCC 构建 | `.codex/gemini-bloom-audit/v6/evidence/stage05/oracle_diff/gemini_default_build_stderr.log` |
| `GBV6-02-001` | P2 | OPEN | GTest target/RPATH | `.codex/gemini-bloom-audit/v6/evidence/stage02/gtest/bloom_test_target_stderr.log` |
| `GBV6-02-002` | P2 | OPEN | TCL expected gaps exit code | `.codex/gemini-bloom-audit/v6/evidence/stage02/tcl/tcl_summary.md` |
| `GBV6-00-001` | P3 | OPEN | DESIGN fixture 证据缺口 | `.codex/gemini-bloom-audit/v6/evidence/stage00/stderr.log` |
| `GBV6-00-002` | P3 | OPEN | 源注释/DESIGN 冲突 | `.codex/gemini-bloom-audit/v6/evidence/stage00/stdout.log` |
| `GBV6-03-003` | P3 | OPEN | TCL 测试命名/注释不准 | `.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md` |

## P1

### GBV6-07-001

- 影响：私有 `BF.LOADCHUNK` 导入流程可以生成可查询但存在 inserted-item false negative 的 completed filter，破坏 Bloom Filter 的“不应对已插入元素返回不存在”基本数据完整性。
- 相关文件/函数：`modules/gemini-bloom/src/bloom_commands.cc` `CmdLoadchunk`，`modules/gemini-bloom/src/bloom_rdb.cc` wire header load。
- Expected：乱序、重复或缺失 layer chunk 不应清除 Loading，也不应生成可查询且有 false negative 的 filter。
- Actual：header 加 final chunk 或重复 first chunk 可以完成 key，`BF.CARD` 报 20，但部分 inserted item false negative。
- 复现：运行 `.codex/gemini-bloom-audit/v6/evidence/stage07/crash_or_failure_repro/README.md` 中的 `stage07_fuzz_fault_safety.py --seed 2970124295`，查看 `failure_rows.json` 的 `cursor_skip_to_final_exposes_false_negatives` 与 `repeat_first_chunk_for_all_layers`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage07/findings.md`，`.codex/gemini-bloom-audit/v6/evidence/stage07/stage07_fuzz_results.json`。
- 建议：记录每层 chunk completion，强制 cursor 顺序或完成 bitmap，只有所有 layer exactly once 后清除 Loading。

### GBV6-07-002

- 影响：half-loaded key 可经 RDB/AOF/fullsync 暴露为 completed filter，导致重启或复制后出现可读 false-negative filter，属于持久化/复制数据完整性风险。
- 相关文件/函数：`modules/gemini-bloom/src/bloom_rdb.cc` `RdbSaveBloom`/`RdbLoadBloom`/`AofRewriteBloom`，`modules/gemini-bloom/src/sb_chain.h` Loading flag。
- Expected：half-loaded key 不应通过 RDB/AOF/fullsync 暴露为 completed filter。
- Actual：header-only Loading key 在 restart/fullsync 后成为普通 `MBbloom--`，`BF.CARD` 报 40，但 inserted item 40/40 false negative。
- 复现：`.codex/gemini-bloom-audit/v6/evidence/stage07/crash_or_failure_repro/README.md` 的 `persist_half_loaded_rdb` / `persist_half_loaded_aof_no_preamble`；fullsync 影响见 `.codex/gemini-bloom-audit/v6/evidence/stage09/replica/loading_partial_fullsync.log`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage07/findings.md`，`.codex/gemini-bloom-audit/v6/evidence/stage09/replica/loading_partial_fullsync.log`。
- 建议：禁止 Loading filter 被 RDB/AOF rewrite/fullsync 序列化，或持久化 loading/chunk metadata 并在加载后继续拒绝查询；也可定义并实现 incomplete key 丢弃语义。

## P2

### GBV6-03-001

- 影响：恶意 RDB 或 `BF.LOADCHUNK` wire header 可以表达单层 2GB 以上但总量不超过 4GB 的 filter metadata，绕过 DESIGN.md 声明的 per-layer data-size 边界；Stage 03 未声称已触发崩溃。
- 相关文件/函数：`ValidateLayerFields`、`BloomLayer::ReadFrom`、`BloomLayer::FromWireMeta`。
- Expected：RDB/wire 非信任输入每层 data size <= 2GB。
- Actual：共享 validator 只约束 total data size <= 4GB，未约束单层 <= 2GB。
- 复现：静态证据见 `.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md`；Stage 07 静态复核见 `.codex/gemini-bloom-audit/v6/evidence/stage07/static_inspection.log`。
- 证据：`.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md`，`.codex/gemini-bloom-audit/v6/evidence/stage07/static_inspection.log`。
- 建议：共享 `kMaxLayerDataSize` 并加入 `ValidateLayerFields()`。

### GBV6-03-002

- 影响：导入的 RDB/wire state 可携带超出命令/config 入口限制的 expansion factor，后续增长可能放大资源请求，违反统一资源边界。
- 相关文件/函数：`ScalingBloomFilter::ReadFrom`、`DeserializeHeader`、`GrowIfNeeded`。
- Expected：RDB/wire expansionFactor 也应遵守 `kMaxExpansion=32768`。
- Actual：RDB/wire 只拒绝 0 或超过 unsigned 范围，接受 `32769` 和 `UINT_MAX`。
- 复现：`.codex/gemini-bloom-audit/v6/evidence/stage07/crash_or_failure_repro/README.md` 的 `expansion_over_limit` 与 `expansion_uint_max`。
- 证据：`.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md`，`.codex/gemini-bloom-audit/v6/evidence/stage07/stage07_fuzz_results.json`。
- 建议：RDB/wire header validation 中拒绝 `> kMaxExpansion`。

### GBV6-05-001

- 影响：Linux/GCC 默认构建不能通过，Stage 05-10 的 Linux runtime 验证必须使用 audit-only `-include climits` workaround，因此默认构建健康度不能声明 PASS。
- 相关文件/函数：`modules/gemini-bloom/src/bloom_rdb.cc` 使用 `UINT_MAX`。
- Expected：Linux/GCC 默认 build 通过。
- Actual：默认 build 因缺 `<climits>` 失败；Stage 05-10 runtime 使用 audit-only `-include climits`。
- 复现：证据见 `.codex/gemini-bloom-audit/v6/evidence/stage05/oracle_diff/gemini_default_build_stderr.log`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage05/oracle_diff/gemini_default_build_stderr.log`。
- 建议：在 `bloom_rdb.cc` 直接 include `<climits>`，然后重跑默认 Linux/GCC build。

### GBV6-02-001

- 影响：`cmake --build ... --target bloom_test` 不是自包含 PASS 命令，macOS 环境会因 GTest dylib RPATH 缺失失败，影响默认测试 gate 的可复现性。
- 相关文件/命令：`modules/gemini-bloom/CMakeLists.txt` 的 `bloom_test` aggregate target；命令 `cmake --build /private/tmp/gemini-bloom-stage02-build.G7fAub -j10 --target bloom_test`。
- Expected：`bloom_test` target 能构建并执行三个 GTest binary，exit code 为 0。
- Actual：target 尝试执行 `bloom_filter_test` 时 `dyld: Library not loaded: @rpath/libgtest_main.1.11.0.dylib`，direct GTest 在设置 `DYLD_LIBRARY_PATH=/opt/anaconda3/lib` 后 114/114 PASS。
- 复现：按 Stage 02 记录运行 `cmake --build /private/tmp/gemini-bloom-stage02-build.G7fAub -j10 --target bloom_test`，或查看 `.codex/gemini-bloom-audit/v6/agents/stage02/findings.md`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage02/gtest/bloom_test_target_stderr.log`，`.codex/gemini-bloom-audit/v6/evidence/stage02/gtest/gtest_summary.md`。
- 建议：设置 GTest-linked test binaries 的 RPATH、改为静态链接 GTest，或让 target 执行时注入已解析的 GTest library path。

### GBV6-02-002

- 影响：TCL 默认命令会把 DESIGN_INTENDED expected gaps 计入失败并返回非零 exit code，CI 或开发者可能误判正常设计边界为实现失败。
- 相关文件/命令：`modules/gemini-bloom/tests/tcl/bloom_test.tcl`；命令 `tclsh modules/gemini-bloom/tests/tcl/bloom_test.tcl <redis_bloom.so>`。
- Expected：RESP3 unsupported 与 RedisBloom byte-offset SCANDUMP cursor 这类 expected gaps 应标为 skip/xfail，默认 harness exit code 不应因为已知设计边界变成失败。
- Actual：TCL summary 为 `144 passed, 6 failed`，6 个失败均是 expected gaps，但脚本 exit code 为 6。
- 复现：运行 Stage 02 的 TCL command，或查看 `.codex/gemini-bloom-audit/v6/evidence/stage02/tcl/tcl_summary.md` 与 `.codex/gemini-bloom-audit/v6/evidence/stage02/tcl/bloom_test_tcl_stdout.log`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage02/tcl/tcl_summary.md`，`.codex/gemini-bloom-audit/v6/evidence/stage02/tcl/bloom_test_tcl_stdout.log`。
- 建议：把 expected gaps 作为 expected-fail/skip 统计，或将负向兼容探针从默认 CI 命令拆出。

## P3

### GBV6-00-001

- 影响：DESIGN.md 声称的 checked-in RedisBloom v2.4.20 fixture/corpus 证据不能从当前仓库路径复现，削弱文档中“fixtures 存在”的可追溯性；Stage 05 仍另行提供 runtime oracle 证据。
- 相关文件/命令：`modules/gemini-bloom/DESIGN.md` fixture 声明；命令 `ls -ld modules/gemini-bloom/tests/compat/redisbloom-2.4.20`。
- Expected：DESIGN.md 指向的 `tests/compat/redisbloom-2.4.20/` 路径存在并包含 RDB fixture 和期望结果，或文档指向实际证据位置。
- Actual：该路径不存在，仓库静态 inventory 未找到对应 fixtures。
- 复现：运行 Stage 00 的 path check，或查看 `.codex/gemini-bloom-audit/v6/evidence/stage00/stderr.log`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage00/stderr.log`，`.codex/gemini-bloom-audit/v6/agents/stage00/findings.md`。
- 建议：添加对应 fixtures/expected results，或修正 DESIGN.md 说明 RedisBloom corpus 证据的真实位置和复现方式。

### GBV6-00-002

- 影响：源码注释把 SCANDUMP/LOADCHUNK 误导为 RedisBloom cross-implementation compatibility surface，和最高优先级 DESIGN.md 的私有协议边界冲突，可能导致维护者误改或误宣称兼容。
- 相关文件/命令：`modules/gemini-bloom/src/sb_chain.h` 注释；命令 `rg -n "RedisBloom|SCANDUMP|LOADCHUNK" modules/gemini-bloom/src/sb_chain.h modules/gemini-bloom/DESIGN.md`。
- Expected：源码注释应说明 gemini 使用私有 layer-index cursor/header 协议，与 RedisBloom SCANDUMP/LOADCHUNK 不互通。
- Actual：`sb_chain.h` 注释称 wire-format structures match RedisBloom binary protocol for cross-implementation compatibility。
- 复现：查看 Stage 00 static inspection output `.codex/gemini-bloom-audit/v6/evidence/stage00/stdout.log`。
- 证据：`.codex/gemini-bloom-audit/v6/evidence/stage00/stdout.log`，`.codex/gemini-bloom-audit/v6/agents/stage00/findings.md`。
- 建议：修正 `sb_chain.h` 注释，使其与 DESIGN.md 的私有协议边界一致。

### GBV6-03-003

- 影响：TCL 测试名和注释会让读者误以为 per-layer data-size rejection 已覆盖，同时保留 stale `512MB` 描述；这会掩盖 `GBV6-03-001` 的真实覆盖缺口。
- 相关文件/命令：`modules/gemini-bloom/tests/tcl/bloom_test.tcl`；命令 `rg -n "Per-layer data size cap|512MB|BloomLayer::Create rejects extremely large capacity" modules/gemini-bloom/tests/tcl/bloom_test.tcl`。
- Expected：测试名称、注释和断言应一致；若测试声称覆盖 per-layer data-size cap，就应验证超限被拒绝，并与 DESIGN.md 的 2GB 边界一致。
- Actual：测试名/注释声称 per-layer cap / 512MB，但实际断言验证 `BF.RESERVE` 在 `kMaxCapacity` 边界成功。
- 复现：查看 `.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md` 中的 `GBV6-03-003`。
- 证据：`.codex/gemini-bloom-audit/v6/agents/stage03/potential_findings.md`。
- 建议：重命名该 TCL 测试为“max command capacity succeeds”，或添加真正的超限拒绝测试，并把 stale `512MB` 注释更新为当前 DESIGN.md 的 2GB 边界。

## Blockers / degraded coverage

Blocker 和 NOT_VERIFIED 项集中列在 `08_测试覆盖与未覆盖.md`，证据映射见 `.codex/gemini-bloom-audit/v6/evidence/stage11/blocked_not_verified_matrix.md`。
